
  # Web UI for Token Calculation

  This is a code bundle for Web UI for Token Calculation. The original project is available at https://www.figma.com/design/7E7I1SFgh3nuR3A16ha4hL/Web-UI-for-Token-Calculation.

  ## Running the code

  Run `pnpm install` to install the dependencies.

  Run `pnpm dev` to start the development server.
